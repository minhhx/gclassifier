-------- How to fork

$git clone git@github.com:minhhx/gclassifier.git

$ cd gclassifier
$ git remote add upstream git://github.com/octocat/gclassifier.git
$ git fetch upstream

$ git push origin master

$ git fetch upstream
$ git merge upstream/master

---------- How to commit
$ git init

$ git add README
$ git commit -m 'first commit'

$ git remote add origin git@github.com:minhhx/gclassifier.git
$ git push origin master

--------- How to get update
git fetch origin
git pull origin
